packages:
  - "apps/*"
  - "packages/*"

# Build-script approvals (pnpm blocks dependency install scripts by default).
allowBuilds:
  '@parcel/watcher': true
  '@swc/core': true
  sharp: true
  unrs-resolver: true
  # Transitive dev/test dep pulled in via shadcn; not used at runtime.
  msw: false
